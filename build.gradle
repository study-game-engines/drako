buildscript {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}


plugins {
    id "featurea" version "$featurea_version" apply false
}

subprojects {
    repositories {
        mavenLocal()
    }
    apply plugin: "featurea"
}
